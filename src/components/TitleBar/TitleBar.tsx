import './TitleBar.css';

const TitleBar = () => (
    <div className="title-bar">
        <div className="title-bar__item">Close</div>
        <div className="title-bar__item">Minimize</div>
    </div>
);

export default TitleBar;